# MailMind
AutoMail AI is an intelligent email automation system that reads business enquiry emails, generates context-aware replies using Ollama, and sends responses automatically via SMTP. It reduces manual effort, improves response time, and works locally without external APIs.
